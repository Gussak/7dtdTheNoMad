# run
cd ZZ-010 Ghussak - Tweaks
clear;./prepareForRelease.sh
# change all files here recursively to RW, so copying below wont fail (or just delete all mod's folders here to copy all fresh below)
# open '_ReleasePackageFiles_.IgnoreOnBackup/Mods/' in browser and copy all folders to this git local repo replacing files

