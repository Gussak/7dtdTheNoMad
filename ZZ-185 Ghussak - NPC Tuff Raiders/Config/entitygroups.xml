<GhussakTweaks>
<!--
	HELPGOOD: this is because the new XBow LBow use explosive arrows now, and npcHarley (that should be RaiderHarley as is bandit), has spawn default probability of only 0.05, so this makes it as probable as the others, so the foe explosive arrows will happen more often.
-->
	<removeattribute xpath="/entitygroups/entitygroup[starts-with(@name,'npcBanditsAmbushRangedGroupGS')]/entity[contains(@name,'Bow')]/@prob"/>
</GhussakTweaks>
